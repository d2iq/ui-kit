import { Meta } from "@storybook/addon-docs";
import { ColorPalette, ColorItem } from "@storybook/addon-docs";
import { themeColors, myColors, designSystemColors } from "../colors.ts";

<Meta title="DesignTokens/Colors" />

# Colors

## Design System Colors

<ColorPalette>
  {designSystemColors.map(item => (
    <ColorItem
      key={item.title}
      title={item.title}
      subtitle={item.subtitle}
      colors={item.colors}
    />
  ))}
</ColorPalette>

## Theme Colors

<ColorPalette>
  {themeColors.map(item => (
    <ColorItem
      key={item.title}
      title={item.title}
      subtitle={item.subtitle}
      colors={item.colors}
    />
  ))}
</ColorPalette>
