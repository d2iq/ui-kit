import { css } from "@emotion/css";

export const buttonReset = css`
  background: none;
  border: 0;
  color: inherit;
  font: inherit;
  line-height: normal;
  overflow: visible;
  padding: 0;
  text-align: inherit;

  &::-moz-focus-inner {
    border: 0;
    padding: 0;
  }
`;
